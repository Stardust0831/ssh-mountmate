[unrelated-cloud]
type = s3
provider = Other
access_key_id = preserved-access-key
secret_access_key = preserved-secret

[legacy-password]
type = sftp
host = stale.example
user = stale-user
pass = stale-obscured-secret

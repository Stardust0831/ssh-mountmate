# SSH MountMate v0.2.3-rc.4

Pre-release UI polish and language-switch fix.

- Fix the main mount list disappearing after changing language in Settings.
- Improve Windows DPI handling and raise the default/card font sizes for better readability on lower-resolution displays.
- Make disabled connection-card buttons show a clear reason when clicked, instead of only appearing grey.

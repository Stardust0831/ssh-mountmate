# SSH MountMate v0.2.3-rc.3

Pre-release for onboarding, settings, and documentation cleanup.

- Show an empty-state prompt with an Add config button when no mount configs exist.
- Split the combined README into English `README.md` and Chinese `README.zh-CN.md`.
- Remove mount/unmount concurrency from Settings; batch mount is fixed at 4 workers and batch unmount at 8 workers.
- Move Settings explanations onto dedicated `?` help icons, with expanded VFS cache mode descriptions.
- Rename the max cache size default to `default (no size limit)` to avoid implying that VFS cache is disabled.

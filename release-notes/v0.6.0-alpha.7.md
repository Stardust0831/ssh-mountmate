# SSH MountMate v0.6.0-alpha.7

This prerelease adds a separate Lustre inode quota progress bar alongside storage capacity.

- Show project inode usage and hard limits on mounted connection cards.
- Keep the indicator unavailable for unlimited inode quotas and non-Lustre filesystems.

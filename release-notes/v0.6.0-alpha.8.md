# SSH MountMate v0.6.0-alpha.8

This prerelease adds Lustre inode quota visibility to mounted connection cards.

- Show a separate inode progress bar when a finite project inode quota is available.
- Cap the visual progress at 100% while retaining the actual usage values.
- Probe the resolved Lustre path before falling back to the filesystem mountpoint.

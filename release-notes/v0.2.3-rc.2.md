# SSH MountMate v0.2.3-rc.2

Pre-release follow-up for issue #5.

- Also repair Windows ACLs on private key copies placed in `~/.ssh`.
- This should address `failed to read private key file: Access is denied` after using "Copy key to ~/.ssh".
